package compression

import "io"

// Compress writes to output the result of compressing the contents of input.
func Compress(input io.Reader, output io.Writer) error {
	// Compress files
	return nil
}
